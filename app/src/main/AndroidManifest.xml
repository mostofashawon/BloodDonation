<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.blooddonation.Main">

    <uses-permission-sdk-23 android:name="android.permission.SEND_SMS"></uses-permission-sdk-23>


    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.blooddonation.View.Message"></activity>
        <activity android:name="com.blooddonation.View.DonerList" />
        <activity android:name="com.blooddonation.Presenter.DonerGroup" />
        <activity android:name="com.blooddonation.Model.SignUp" />
        <activity android:name=".Confirmation" />
        <activity android:name=".Login" />
        <activity android:name=".WelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>